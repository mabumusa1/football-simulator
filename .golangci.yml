version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  default: standard

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 3

