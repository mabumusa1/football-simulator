# SonarCloud Configuration
sonar.projectKey=mabumusa1_football-simulator
sonar.organization=mabumusa

# Project metadata
sonar.projectName=football-simulator
sonar.projectVersion=1.0

# Source directories
sonar.sources=apps/api,apps/consumer

# Encoding
sonar.sourceEncoding=UTF-8

# Go coverage report
sonar.go.coverage.reportPaths=coverage.out

# Exclusions from analysis (test files, vendor)
sonar.exclusions=**/*_test.go,**/vendor/**

# Coverage exclusions - only Go code should be in coverage metrics
sonar.coverage.exclusions=**/*.yaml,**/*.yml,**/Dockerfile,**/*.mod,**/*.sum,**/main.go

# Duplication exclusions - re-export wrapper files are intentional architectural patterns
sonar.cpd.exclusions=apps/api/internal/domain/event.go,apps/consumer/internal/domain/event.go,apps/consumer/internal/domain/engagement.go

# Test file patterns
sonar.test.inclusions=**/*_test.go
